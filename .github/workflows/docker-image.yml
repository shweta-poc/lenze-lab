name: Build & Push an image to dockerhub

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build -t my-app:latest .       
    - name: Docker Login
      uses: docker/login-action@v3.3.0
      with:
        username: stodkar898@gmail.com
        # Password or personal access token used to log against the Docker registry
        password: Qwertyuiop@231997
   
    - name: Tag an image
      run: docker tag my-app:latest stodkar898/lenze-lab
    - name: Push an image to docker hubd     
      run: docker push stodkar898/lenze-lab my-app:latest
